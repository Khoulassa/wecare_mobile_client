enum States { initial, loading, loaded, error }

enum TableName { address, children }

enum IdTyp { order, service }

enum FromScreen { order, main }

enum ActionTyp { add, edit }

enum OrderType {current, completed,waiting,rejected,cancelled,inPayment}

